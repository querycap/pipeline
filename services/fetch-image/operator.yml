name: sys/fetch-image
description: fetch image
version: 0.0.1

inputs:
  contentType: application/json
  schema:
    type: object
    properties:
      uri:
        type: string

outputs:
  contentType: image/*
